import React from "react";

const Edit = (props: any) => {
  const postId = props.match.params.id;
  return (
    <div>
      <h1>Edit UI not done yet. However the endpoint is created already:</h1>
    </div>
  );
};

export default Edit;
